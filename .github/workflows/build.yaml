on: [push]

name: build

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: dtolnay/toolchain@v1
        with:
          toolchain: 1.72.1
          target: wasm32-unknown-unknown
          override: true
          components: clippy
